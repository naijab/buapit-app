<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="FragmentMoreTab" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/grey_600</item>
        <item name="tabIndicatorHeight">2dp</item>
        <item name="tabBackground">?attr/selectableItemBackground</item>
    </style>

    <style name="CardView.White" parent="CardView.Light">
        <item name="cardCornerRadius">@dimen/dimen_none</item>
        <item name="cardElevation">@dimen/dimen_small</item>
        <item name="android:layout_marginTop">@dimen/dimen_normal</item>
        <item name="android:layout_marginBottom">@dimen/dimen_normal</item>
        <item name="android:layout_marginLeft">@dimen/dimen_none</item>
        <item name="android:layout_marginRight">@dimen/dimen_none</item>
        <item name="android:background">@color/colorWhite</item>
    </style>

    <style name="TextPrimary">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="MainToolbarTitle" parent="TextPrimary">
        <item name="android:layout_marginTop">@dimen/dimen_none</item>
        <item name="android:layout_marginBottom">@dimen/dimen_none</item>
        <item name="android:layout_marginLeft">@dimen/dimen_none</item>
        <item name="android:layout_marginRight">@dimen/dimen_none</item>
        <item name="android:textSize">@dimen/font_xx</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="TabLayoutMain">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">60dp</item>
    </style>

</resources>
